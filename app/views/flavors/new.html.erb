<!-- PROBABLY NOT NEEDED SINCE WILL BE DONE FROM ADMIN PANEL -->

<h1>Add a New Flavor</h1>


<%= form_for :flavor, url: flavors_path do |f| %>

<% if @flavor.errors.any? %>
<!-- error checking validation -->
    <div id="flavor_error_explanation">
      <h2>
        <%= pluralize(@flavor.errors.count, "error") %> prohibited
        this flavor from being saved:
      </h2>
      <ul>
        <% @flavor.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
      <% end %>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
<br/>
    <%= f.submit %>


      <% end %>




<%= form_for :flavor, url: flavors_path do |f| %>

<% end %>

<%= link_to 'Back', flavors_path %>
